(()=>{const $=id=>document.getElementById(id),secret=$('opsSecret'),limit=$('opsLimit'),load=$('opsLoad'),forget=$('opsForget'),message=$('opsMessage'),counts=$('opsCounts'),orders=$('opsOrders'),checkouts=$('opsCheckouts'),refunds=$('opsRefunds'),events=$('opsEvents');const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const fmt=s=>{if(!s)return'—';try{return new Date(s).toLocaleString()}catch{return esc(s)}};const badge=s=>`<span class="ops-badge">${esc(s||'—')}</span>`;const money=n=>Number.isFinite(Number(n))?`$${Number(n).toFixed(2)}`:'—';const stored=sessionStorage.getItem('lumen-internal-secret')||'';if(stored)secret.value=stored;function setMessage(text,bad=false){message.textContent=text;message.style.fontWeight=bad?'700':''}function rowsOrEmpty(items,render,cols){return items?.length?items.map(render).join(''):`<tr><td colspan="${cols}">표시할 데이터가 없습니다.</td></tr>`}async function fetchOps(){const key=secret.value.trim();if(!key){setMessage('Internal secret을 입력해 주세요.',true);return}sessionStorage.setItem('lumen-internal-secret',key);load.disabled=true;setMessage('운영 데이터를 불러오는 중입니다…');try{const n=Math.max(1,Math.min(100,Number(limit.value)||25)),res=await fetch(`/api/admin/guardian-operations?limit=${n}&ts=${Date.now()}`,{cache:'no-store',headers:{accept:'application/json','x-lumen-internal-secret':key}});let data={};try{data=await res.json()}catch{}if(!res.ok)throw new Error(data?.error||`HTTP ${res.status}`);const c=data.counts||{};counts.innerHTML=`<article class="ops-card"><span>대기 주문</span><strong>${esc(c.pending_orders??0)}</strong></article><article class="ops-card"><span>고객지원 검토</span><strong class="${Number(c.support_review)>0?'ops-alert':''}">${esc(c.support_review??0)}</strong></article><article class="ops-card"><span>환불 주의</span><strong class="${Number(c.refund_attention)>0?'ops-alert':''}">${esc(c.refund_attention??0)}</strong></article><article class="ops-card"><span>열린 Checkout</span><strong>${esc(c.open_checkouts??0)}</strong></article>`;orders.innerHTML=rowsOrEmpty(data.orders,o=>`<tr><td><strong>${esc(o.id)}</strong></td><td>${esc(o.tier)}</td><td>${money(o.price_usd)}</td><td>${badge(o.payment_status)}</td><td>${badge(o.issuance_status)}</td><td>${o.issuance_serial?`${esc(o.issuance_serial)} / ${esc(o.edition_key||'')}`:'—'}</td><td>${badge(o.refund_status)}</td><td>${badge(o.support_status)}</td><td>${fmt(o.created_at)}</td></tr>`,9);checkouts.innerHTML=rowsOrEmpty(data.checkouts,x=>`<tr><td>${esc(x.checkout_id)}</td><td>${esc(x.guardian_id)}</td><td>${esc(x.provider)}</td><td>${money(x.amount_usd)} ${esc(x.currency||'')}</td><td>${badge(x.status)}</td><td>${esc(x.failure_code||'—')}</td><td>${fmt(x.expires_at)}</td></tr>`,7);refunds.innerHTML=rowsOrEmpty(data.refunds,r=>`<tr><td>${esc(r.refund_job_id)}</td><td>${esc(r.guardian_id)}</td><td>${money(r.amount_usd)} ${esc(r.currency||'')}</td><td>${esc(r.reason)}</td><td>${badge(r.status)}</td><td>${esc(r.last_error||'—')}</td><td>${fmt(r.updated_at)}</td></tr>`,7);events.innerHTML=rowsOrEmpty(data.paymentEvents,e=>`<tr><td>${esc(e.event_id)}</td><td>${esc(e.event_type)}</td><td>${esc(e.guardian_id||'—')}</td><td>${esc(e.provider)}</td><td>${badge(e.status)}</td><td>${esc(e.error_code||'—')}</td><td>${fmt(e.received_at)}</td></tr>`,7);setMessage(`마지막 갱신: ${fmt(data.generatedAt)}`)}catch(e){setMessage(`불러오기 실패: ${e.message||e}`,true)}finally{load.disabled=false}}load?.addEventListener('click',fetchOps);forget?.addEventListener('click',()=>{sessionStorage.removeItem('lumen-internal-secret');secret.value='';setMessage('현재 브라우저 탭의 비밀값을 지웠습니다.')});})();