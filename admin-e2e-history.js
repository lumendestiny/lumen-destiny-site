(()=>{const secret=document.getElementById('opsSecret'),load=document.getElementById('opsLoad'),limit=document.getElementById('opsLimit'),tbody=document.getElementById('opsE2eRuns'),summary=document.getElementById('opsE2eSummary');if(!secret||!load||!tbody)return;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const fmt=s=>{try{return s?new Date(s).toLocaleString():'—'}catch{return esc(s)}};const badge=s=>`<span class="ops-badge">${esc(String(s||'—').toUpperCase())}</span>`;function ids(v){try{const a=JSON.parse(v||'[]');return Array.isArray(a)?a.join(', '):'—'}catch{return'—'}}async function refresh(){const key=secret.value.trim();if(!key)return;summary.textContent='E2E 테스트 이력을 확인하는 중입니다…';const n=Math.max(1,Math.min(100,Number(limit?.value)||25));try{const res=await fetch(`/api/admin/e2e-runs?limit=${n}&ts=${Date.now()}`,{cache:'no-store',headers:{accept:'application/json','x-lumen-internal-secret':key}}),data=await res.json();if(!res.ok)throw new Error(data?.error||`HTTP ${res.status}`);const runs=data.runs||[],countMap=Object.fromEntries((data.counts||[]).map(x=>[x.status,Number(x.count)||0]));tbody.innerHTML=runs.length?runs.map(r=>`<tr><td class="ops-code">${esc(r.run_id)}</td><td>${esc(r.scenario)}</td><td>${badge(r.status)}</td><td>${esc(r.summary||'—')}</td><td class="ops-code">${esc(ids(r.guardian_ids))}</td><td>${fmt(r.created_at)}</td></tr>`).join(''):'<tr><td colspan="6">저장된 E2E 테스트 이력이 없습니다.</td></tr>';const latest=runs[0],latestText=latest?`${String(latest.status).toUpperCase()} · ${latest.scenario} · ${fmt(latest.created_at)}`:'아직 실행 기록 없음';summary.textContent=`최근 ${runs.length}건 · 전체 PASS ${countMap.pass||0} / CHECK ${countMap.check||0} / FAIL ${countMap.fail||0} · 최신: ${latestText}`}catch(e){tbody.innerHTML=`<tr><td colspan="6">E2E 이력 조회 실패: ${esc(e.message||e)}</td></tr>`;summary.textContent=`E2E 이력 조회 실패: ${e.message||e}`}}load.addEventListener('click',()=>setTimeout(refresh,50))})();