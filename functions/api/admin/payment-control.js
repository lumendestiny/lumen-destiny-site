const headers={'Cache-Control':'no-store','Content-Type':'application/json; charset=utf-8','X-Content-Type-Options':'nosniff'};const json=(b,s=200)=>new Response(JSON.stringify(b),{status:s,headers});const auth=(r,e)=>!!e?.LUMEN_INTERNAL_SECRET&&(r.headers.get('x-lumen-internal-secret')||'')===e.LUMEN_INTERNAL_SECRET;const clean=(v,n=1000)=>String(v??'').trim().slice(0,n);const makeId=()=>`PCA-${Date.now()}-${crypto.randomUUID().replace(/-/g,'').slice(0,8).toUpperCase()}`;async function state(env){return await env.GUARDIAN_DB.prepare(`SELECT control_key,state,note,changed_at FROM guardian_payment_control WHERE control_key='checkout' LIMIT 1`).first()}export async function onRequestGet({request,env}){if(!auth(request,env))return json({ok:false,error:'unauthorized'},401);if(!env?.GUARDIAN_DB)return json({ok:false,error:'storage_not_configured'},503);try{const [control,audit]=await Promise.all([state(env),env.GUARDIAN_DB.prepare(`SELECT audit_id,previous_state,new_state,note,changed_at FROM guardian_payment_control_audit WHERE control_key='checkout' ORDER BY changed_at DESC LIMIT 20`).all()]);return json({ok:true,control:control||{control_key:'checkout',state:'hold',note:'Missing control row is treated as fail-closed HOLD',changed_at:null},audit:audit?.results||[]})}catch{return json({ok:false,error:'storage_read_failed'},500)}}export async function onRequestPost({request,env}){if(!auth(request,env))return json({ok:false,error:'unauthorized'},401);if(!env?.GUARDIAN_DB)return json({ok:false,error:'storage_not_configured'},503);let b;try{b=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const target=clean(b?.state,20).toLowerCase(),note=clean(b?.note,1200),confirmation=clean(b?.confirmation,80);if(!['open','hold'].includes(target))return json({ok:false,error:'invalid_state'},400);if(note.length<10)return json({ok:false,error:'operation_note_required'},400);const required=target==='open'?'RESUME GUARDIAN PAYMENTS':'HOLD GUARDIAN PAYMENTS';if(confirmation!==required)return json({ok:false,error:'confirmation_mismatch',requiredConfirmation:required},409);try{const current=await state(env)||{state:'hold'};if(current.state===target)return json({ok:true,unchanged:true,state:target});if(target==='open'){if(env?.LUMEN_PAYMENT_EMERGENCY_HOLD==='true')return json({ok:false,error:'environment_emergency_hold_active'},409);const critical=await env.GUARDIAN_DB.prepare(`SELECT incident_id FROM guardian_payment_incidents WHERE status!='resolved' AND severity='critical' LIMIT 1`).first();if(critical)return json({ok:false,error:'critical_incident_unresolved',incidentId:critical.incident_id},409)}const now=new Date().toISOString(),auditId=makeId();await env.GUARDIAN_DB.batch([env.GUARDIAN_DB.prepare(`INSERT INTO guardian_payment_control(control_key,state,note,changed_at) VALUES('checkout',?,?,?) ON CONFLICT(control_key) DO UPDATE SET state=excluded.state,note=excluded.note,changed_at=excluded.changed_at`).bind(target,note,now),env.GUARDIAN_DB.prepare(`INSERT INTO guardian_payment_control_audit(audit_id,control_key,previous_state,new_state,note,changed_at) VALUES(?,'checkout',?,?,?,?)`).bind(auditId,current.state,target,note,now)]);return json({ok:true,state:target,auditId,changedAt:now})}catch{return json({ok:false,error:'storage_write_failed'},500)}}export async function onRequest(){return json({ok:false,error:'method_not_allowed'},405)}