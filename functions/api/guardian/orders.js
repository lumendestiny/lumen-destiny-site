const json=(body,status=200)=>Response.json(body,{status,headers:{'Cache-Control':'no-store'}});const TIERS={basic:{price:5,limit:100},custom:{price:10,limit:100},rare:{price:50,limit:5},legendary:{price:100,limit:1}};
export async function onRequestPost({request,env}){let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const tier=String(body?.tier||''),name=String(body?.name||'').trim().slice(0,40),wishType=String(body?.wishType||'custom').slice(0,30);if(!TIERS[tier])return json({ok:false,error:'invalid_tier'},400);if(!name)return json({ok:false,error:'name_required'},400);if(env?.LUMEN_GUARDIAN_ENABLED!=='true')return json({ok:false,error:'guardian_not_enabled',message:'Guardian server issuance is prepared but not enabled yet.',draft:{tier,name,wishType,price:TIERS[tier].price}},503);return json({ok:false,error:'storage_not_configured'},503)}
export async function onRequest(){return json({ok:false,error:'method_not_allowed'},405)}