const headers={'Cache-Control':'no-store','Content-Type':'application/json; charset=utf-8'};const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers});const TIERS={basic:{price:5,limit:100},custom:{price:10,limit:100},rare:{price:50,limit:5},legendary:{price:100,limit:1}};const clean=(v,max)=>String(v??'').trim().slice(0,max);const makeId=()=>{const d=new Date(),date=`${d.getUTCFullYear()}${String(d.getUTCMonth()+1).padStart(2,'0')}${String(d.getUTCDate()).padStart(2,'0')}`,rand=crypto.randomUUID().replace(/-/g,'').slice(0,10).toUpperCase();return`LG-${date}-${rand}`};
export async function onRequestPost({request,env}){let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const tier=clean(body?.tier,20),name=clean(body?.name,40),wishType=clean(body?.wishType||'custom',30),isGift=body?.isGift===1||body?.isGift===true?1:0,giverName=clean(body?.giverName,40),recipientName=clean(body?.recipientName,40),giftMessage=clean(body?.giftMessage,200),campaignId=clean(body?.campaignId,80),targetDate=clean(body?.targetDate,20);if(!TIERS[tier])return json({ok:false,error:'invalid_tier'},400);if(!name)return json({ok:false,error:'name_required'},400);if(isGift&&!recipientName)return json({ok:false,error:'recipient_required'},400);if(env?.LUMEN_GUARDIAN_ENABLED!=='true')return json({ok:false,error:'guardian_not_enabled',message:'Guardian server issuance is prepared but not enabled yet.',draft:{tier,name,wishType,price:TIERS[tier].price,isGift,recipientName,campaignId}},503);if(!env?.GUARDIAN_DB)return json({ok:false,error:'storage_not_configured',message:'D1 binding GUARDIAN_DB is required.'},503);const id=makeId(),token=crypto.randomUUID(),createdAt=new Date().toISOString(),meta=TIERS[tier];try{await env.GUARDIAN_DB.prepare(`INSERT INTO guardian_orders (id,tier,price_usd,edition_limit,display_name,wish_type,payment_status,issuance_status,created_at,verification_token,is_gift,giver_name,recipient_name,gift_message,campaign_id,target_date) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,tier,meta.price,meta.limit,name,wishType,'pending','pending',createdAt,token,isGift,giverName||null,recipientName||null,giftMessage||null,campaignId||null,targetDate||null).run()}catch{return json({ok:false,error:'storage_write_failed'},500)}return json({ok:true,order:{id,tier,priceUsd:meta.price,editionLimit:meta.limit,displayName:name,wishType,paymentStatus:'pending',issuanceStatus:'pending',createdAt,isGift:!!isGift,giverName,recipientName,campaignId,targetDate},verifyUrl:`/guardian-verify.html?id=${encodeURIComponent(id)}`},201)}
export async function onRequest(){return json({ok:false,error:'method_not_allowed'},405)}