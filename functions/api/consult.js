const json=(body,status=200)=>Response.json(body,{status,headers:{'Cache-Control':'no-store'}});
export async function onRequestPost({request,env}){let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const topic=String(body?.topic||'').slice(0,40),question=String(body?.question||'').trim().slice(0,1200),lang=String(body?.lang||'ko').slice(0,8),context=body?.context&&typeof body.context==='object'?body.context:{};if(!question)return json({ok:false,error:'question_required'},400);const safeContext={name:String(context.name||'').slice(0,40),birthDate:String(context.birthDate||'').slice(0,20),birthTime:String(context.birthTime||'').slice(0,10),calendarType:String(context.calendarType||'').slice(0,12)};if(env?.LUMEN_AI_ENABLED!=='true')return json({ok:false,error:'ai_not_enabled',message:'AI consultation backend is prepared but not enabled yet.',received:{topic,lang,context:safeContext}},503);return json({ok:false,error:'provider_not_configured'},503)}
export async function onRequest(){return json({ok:false,error:'method_not_allowed'},405)}