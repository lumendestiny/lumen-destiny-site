(()=>{const form=document.getElementById('verifyForm');const idEl=document.getElementById('verifyId');const result=document.getElementById('verifyResult');const title=document.getElementById('verifyTitle');const status=document.getElementById('verifyStatus');const body=document.getElementById('verifyBody');const qs=new URLSearchParams(location.search);const requested=qs.get('id')||'';if(requested)idEl.value=requested;function render(id){result.hidden=false;title.textContent='Guardian 발급 준비번호';status.textContent='MVP PREVIEW';body.innerHTML='<article><span>확인 번호</span><h3>'+id.replace(/[<>]/g,'')+'</h3><p>현재는 발급번호 형식과 인증 화면을 검증하는 단계입니다. 실제 발급 상태 확인은 서버 인증 계층 연결 후 활성화됩니다.</p></article>'}form.addEventListener('submit',e=>{e.preventDefault();render(idEl.value.trim())});if(requested)render(requested)})();