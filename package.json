{
  "name": "lumen-destiny",
  "version": "1.0.0",
  "private": true,
  "description": "Lumen Destiny web app packaged for Android with Capacitor",
  "scripts": {
    "android:prepare": "node scripts/android-prepare.mjs",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:bundle": "npm run android:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
