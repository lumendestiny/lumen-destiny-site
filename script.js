const yearEl=document.getElementById('year');if(yearEl)yearEl.textContent=new Date().getFullYear();
const form=document.getElementById('sajuForm');
if(form){form.addEventListener('submit',function(e){e.preventDefault();const name=document.getElementById('userName').value.trim()||'당신';const date=document.getElementById('birthDate').value;const time=document.getElementById('birthTime').value;const calendar=document.getElementById('calendarType').value==='lunar'?'음력':'양력';const genderMap={male:'남성',female:'여성',other:'선택 안 함'};const gender=genderMap[document.getElementById('gender').value];const result=document.getElementById('sajuResult');const seed=(date.replaceAll('-','').split('').reduce((a,b)=>a+Number(b),0)+(time?Number(time.replace(':','')):7));const elements=['목(木)','화(火)','토(土)','금(金)','수(水)'];const strengths=['새로운 일을 시작하는 추진력','사람과 관계를 이어가는 따뜻함','꾸준히 기반을 만드는 안정감','판단하고 정리하는 결단력','상황을 읽고 유연하게 움직이는 힘'];const element=elements[seed%5];const strength=strengths[seed%5];result.innerHTML=`<h3>${name}님의 첫 명운 결과</h3><p><span class="result-highlight">현재 테스트 오행 포인트: ${element}</span></p><p>${name}님에게는 <strong>${strength}</strong>을 돌아보는 해석이 잘 어울립니다.</p><p>입력 기준: ${calendar} · ${date}${time?' · '+time:''} · ${gender}</p><p><strong>오늘의 한 걸음</strong><br>결과를 미래의 확정된 답으로 보기보다, 오늘 한 가지 작은 선택을 더 좋은 방향으로 바꿔보세요.</p><p style="opacity:.78;font-size:.9rem">※ 지금은 화면과 사용 흐름을 확인하기 위한 V1 테스트 결과입니다. 정식 명리 계산 및 AI 해설은 다음 단계에서 연결합니다.</p>`;result.hidden=false;result.scrollIntoView({behavior:'smooth',block:'center'});});}