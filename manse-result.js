import { calculateSaju, lunarToSolar } from 'https://esm.sh/@fullstackfamily/manseryeok@latest';
const yearEl=document.getElementById('year');if(yearEl)yearEl.textContent=new Date().getFullYear();
const loading=document.getElementById('manseLoading'),errorBox=document.getElementById('manseError'),content=document.getElementById('manseContent');
const STEM_ELEMENT={갑:'목',을:'목',병:'화',정:'화',무:'토',기:'토',경:'금',신:'금',임:'수',계:'수'};
const STEM_YANG={갑:1,을:0,병:1,정:0,무:1,기:0,경:1,신:0,임:1,계:0};
const BRANCH_ELEMENT={자:'수',축:'토',인:'목',묘:'목',진:'토',사:'화',오:'화',미:'토',신:'금',유:'금',술:'토',해:'수'};
const HIDDEN={자:['계'],축:['기','계','신'],인:['갑','병','무'],묘:['을'],진:['무','을','계'],사:['병','무','경'],오:['정','기'],미:['기','정','을'],신:['경','임','무'],유:['신'],술:['무','신','정'],해:['임','갑']};
const ELEMENT_LABEL={목:'목(木)',화:'화(火)',토:'토(土)',금:'금(金)',수:'수(水)'};
const ELEMENT_READING={목:'성장과 시작, 확장하려는 힘',화:'표현과 추진, 드러내는 에너지',토:'안정과 축적, 중심을 잡는 힘',금:'판단과 정리, 기준을 세우는 힘',수:'관찰과 유연함, 흐름을 읽는 힘'};
const GEN={목:'화',화:'토',토:'금',금:'수',수:'목'},CONTROL={목:'토',토:'수',수:'화',화:'금',금:'목'};
function showError(message){loading.hidden=true;content.hidden=true;errorBox.hidden=false;errorBox.innerHTML=`<h2>결과를 계산하지 못했습니다.</h2><p>${message}</p><a class="button primary" href="/#analysis">입력 화면으로 돌아가기</a>`;}
function splitPillar(p){if(!p||p.length<2)return ['?','?'];return [p[0],p[1]];}
function tenGod(day,target){if(!STEM_ELEMENT[day]||!STEM_ELEMENT[target])return '—';const me=STEM_ELEMENT[day],t=STEM_ELEMENT[target],same=STEM_YANG[day]===STEM_YANG[target];if(me===t)return same?'비견':'겁재';if(GEN[me]===t)return same?'식신':'상관';if(CONTROL[me]===t)return same?'편재':'정재';if(CONTROL[t]===me)return same?'편관':'정관';if(GEN[t]===me)return same?'편인':'정인';return '—';}
function countElements(pillars){const counts={목:0,화:0,토:0,금:0,수:0};pillars.filter(Boolean).forEach(p=>{const [s,b]=splitPillar(p);if(STEM_ELEMENT[s])counts[STEM_ELEMENT[s]]++;if(BRANCH_ELEMENT[b])counts[BRANCH_ELEMENT[b]]++;});return counts;}
function buildReading(counts){const ranked=Object.entries(counts).sort((a,b)=>b[1]-a[1]);const top=ranked[0],low=ranked[ranked.length-1];return `<p><strong>${ELEMENT_LABEL[top[0]]}</strong>의 표면 비중이 가장 높게 나타납니다. 전통적 오행 관점에서는 ${ELEMENT_READING[top[0]]}과 관련된 성향을 살펴볼 수 있습니다.</p><p>상대적으로 <strong>${ELEMENT_LABEL[low[0]]}</strong>의 표면 비중은 낮습니다. 이것이 곧 부족함이나 불운을 뜻하는 것은 아닙니다.</p><p><strong>오늘의 한 걸음</strong><br>강하게 나타난 성향을 어디에 쓰고 약하게 나타난 부분을 어떤 습관으로 보완할지 한 가지씩 정해보세요.</p>`;}
function getInput(){const p=new URLSearchParams(location.search);let birthDate=p.get('birthDate');if(!birthDate&&p.get('birthYear'))birthDate=`${p.get('birthYear')}-${p.get('birthMonth')||'01'}-${p.get('birthDay')||'01'}`;if(birthDate)return {name:p.get('name')||'당신',birthDate,birthTime:p.get('birthTime')||'',calendarType:p.get('calendarType')||'solar',isLeapMonth:p.get('isLeapMonth')==='1',gender:p.get('gender')||'other'};return null;}
const pairKey=(a,b)=>[a,b].sort().join('');
const REL={합:new Set(['자축','인해','묘술','진유','사신','오미'].map(x=>pairKey(x[0],x[1]))),충:new Set(['자오','축미','인신','묘유','진술','사해'].map(x=>pairKey(x[0],x[1]))),파:new Set(['자유','축진','인해','묘오','사신','미술'].map(x=>pairKey(x[0],x[1]))),해:new Set(['자미','축오','인사','묘진','신해','유술'].map(x=>pairKey(x[0],x[1])))};
const PUNISH_GROUPS=[['인','사','신'],['축','술','미']];
function branchRelations(branches,names){const out=[];for(let i=0;i<branches.length;i++)for(let j=i+1;j<branches.length;j++){if(!branches[i]||!branches[j])continue;const key=pairKey(branches[i],branches[j]);for(const [type,set] of Object.entries(REL))if(set.has(key))out.push(`${names[i]} ${branches[i]} ↔ ${names[j]} ${branches[j]} · ${type}`);if(branches[i]===branches[j]&&['진','오','유','해'].includes(branches[i]))out.push(`${names[i]} ${branches[i]} ↔ ${names[j]} ${branches[j]} · 자형`);}for(const group of PUNISH_GROUPS){const present=group.filter(x=>branches.includes(x));if(present.length>=2)out.push(`${present.join('·')} · 형 관계`);}if(branches.includes('자')&&branches.includes('묘'))out.push('자·묘 · 형 관계');return [...new Set(out)];}
try{const input=getInput();if(!input)throw new Error('입력 정보가 없습니다.');const [y,m,d]=input.birthDate.split('-').map(Number);let sy=y,sm=m,sd=d;if(input.calendarType==='lunar'){const converted=lunarToSolar(y,m,d,Boolean(input.isLeapMonth));sy=converted.solar.year;sm=converted.solar.month;sd=converted.solar.day;}let hour,minute=0;if(input.birthTime)[hour,minute]=input.birthTime.split(':').map(Number);const saju=input.birthTime?calculateSaju(sy,sm,sd,hour,minute,{applyTimeCorrection:false}):calculateSaju(sy,sm,sd,12,0,{applyTimeCorrection:false});const pillars=[saju.yearPillar,saju.monthPillar,saju.dayPillar,input.birthTime?saju.hourPillar:null];const names=['년주','월주','일주','시주'];document.getElementById('resultTitle').textContent=`${input.name}님의 만세력 결과`;document.getElementById('resultIntro').textContent=`기준 양력 ${sy}년 ${sm}월 ${sd}일${input.birthTime?` · ${input.birthTime}`:' · 출생시간 미입력'}`;const hanjas=[saju.yearPillarHanja,saju.monthPillarHanja,saju.dayPillarHanja,input.birthTime?saju.hourPillarHanja:'—'];document.getElementById('pillarGrid').innerHTML=names.map((n,i)=>`<article class="pillar-card"><span>${n}</span><strong>${pillars[i]||'미입력'}</strong><em>${hanjas[i]||'—'}</em></article>`).join('');document.getElementById('timeCorrection').textContent=input.birthTime?'입력한 시각을 기준으로 시주를 계산했습니다.':'출생시간을 입력하지 않아 시주는 표시하지 않습니다.';
const dayStem=splitPillar(saju.dayPillar)[0];document.getElementById('tenGodGrid').innerHTML=pillars.map((p,i)=>{if(!p)return `<div><span>${names[i]}</span><strong>미입력</strong></div>`;const [s,b]=splitPillar(p);return `<div><span>${names[i]} 천간 ${s}</span><strong>${i===2?'일간(본인)':tenGod(dayStem,s)}</strong><small>지지 ${b}의 본기 ${HIDDEN[b]?.[0]||'—'} · ${tenGod(dayStem,HIDDEN[b]?.[0])}</small></div>`;}).join('');document.getElementById('hiddenStemGrid').innerHTML=pillars.map((p,i)=>{if(!p)return `<div><span>${names[i]}</span><strong>미입력</strong></div>`;const b=splitPillar(p)[1],hs=HIDDEN[b]||[];return `<div><span>${names[i]} · ${b}</span><strong>${hs.join(' · ')||'—'}</strong><small>${hs.map(h=>`${h}(${tenGod(dayStem,h)})`).join(' · ')}</small></div>`;}).join('');const branches=pillars.map(p=>p?splitPillar(p)[1]:null);const relations=branchRelations(branches,names);document.getElementById('relationGrid').innerHTML=relations.length?relations.map(r=>`<div class="relation-item">${r}</div>`).join(''):'<div class="relation-item muted">네 지지 사이에서 대표적인 합·충·형·파·해 관계가 두드러지지 않습니다.</div>';
const counts=countElements(pillars),total=Object.values(counts).reduce((a,b)=>a+b,0)||1;document.getElementById('elementChart').innerHTML=Object.entries(counts).map(([k,v])=>{const pct=Math.round(v/total*100);return `<div class="element-row"><div class="element-label"><span>${ELEMENT_LABEL[k]}</span><strong>${v}자 · ${pct}%</strong></div><div class="element-track"><span style="width:${pct}%"></span></div></div>`}).join('');const ranked=Object.entries(counts).sort((a,b)=>b[1]-a[1]);document.getElementById('elementSummary').innerHTML=`<div><span>가장 많이 보이는 오행</span><strong>${ELEMENT_LABEL[ranked[0][0]]}</strong></div><div><span>일간 · 나를 나타내는 천간</span><strong>${dayStem}</strong></div><div><span>계산 기준</span><strong>${input.calendarType==='lunar'?(input.isLeapMonth?'음력 윤달':'음력'):'양력'} → 절기 기준</strong></div>`;document.getElementById('basicReading').innerHTML=buildReading(counts);loading.hidden=true;content.hidden=false;}catch(err){showError(err?.message||'계산 엔진 연결을 확인해주세요.');}
