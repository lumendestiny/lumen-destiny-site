# Lumen Connection Map Web

- Birth date and birth time are used only in-memory for calculation.
- Raw birth information is not written to localStorage by this page.
- The browser stores only display name, relation label, derived Five Elements distribution, relationship score/grade, relation-group override, and timestamps.
- Users can clear the locally stored relationship network from the page.
