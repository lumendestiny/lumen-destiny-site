plugins {
    id 'com.android.application'
}

def releaseKeystorePath = System.getenv('LUMEN_ANDROID_KEYSTORE_PATH')
def releaseKeystorePassword = System.getenv('LUMEN_ANDROID_KEYSTORE_PASSWORD')
def releaseKeyAlias = System.getenv('LUMEN_ANDROID_KEY_ALIAS')
def releaseKeyPassword = System.getenv('LUMEN_ANDROID_KEY_PASSWORD')
def hasReleaseSigning = releaseKeystorePath && releaseKeystorePassword && releaseKeyAlias && releaseKeyPassword

android {
    namespace 'com.lumendestiny.app'
    compileSdk 36

    defaultConfig {
        applicationId 'com.lumendestiny.app'
        minSdk 24
        targetSdk 36
        versionCode 1
        versionName '1.0.0'
    }

    signingConfigs {
        if (hasReleaseSigning) {
            release {
                storeFile file(releaseKeystorePath)
                storePassword releaseKeystorePassword
                keyAlias releaseKeyAlias
                keyPassword releaseKeyPassword
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            if (hasReleaseSigning) {
                signingConfig signingConfigs.release
            }
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
